/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'www.statista.com',
      'mca.org.uk',
      'upload.wikimedia.org'
    ]
  }
};

export default nextConfig;
